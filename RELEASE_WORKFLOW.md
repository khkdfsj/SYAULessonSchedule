# 课表发布流程

## 分支职责

- `main`：线上正式版，内容必须与 bm 的 `/www/server/nginx/html/LessonSchedule/` 一致。
- `test`：下一版本候选版，仅部署到 `/LessonSchedule-test/`，只允许已认证管理员访问。
- 个人开发分支：日常开发与提交，完成后合并到 `test`，不得直接发布带新版本号的代码。

## 带版本号的发布

1. 从最新 `main` 更新 `test`，在 `test` 上修改三处前端版本号；正式后端的 `APP_VERSION` 暂不修改。
2. 使用 `scripts/build-test.ps1` 生成基路径为 `/LessonSchedule-test/` 的 HBuilderX 正式构建。
3. 备份现有测试目录，部署到 bm `/www/server/nginx/html/LessonSchedule-test/`；测试入口由 `deploy/test-index.php` 校验现有管理员身份。
4. 正式入口通过 `deploy/admin-entry.php` 识别现有管理员身份并自动跳转测试版，普通用户仍使用正式版；管理员在企业微信中验收登录、课表、设置、弹窗、反馈、课程详情和弱网首屏。
5. 只有在项目负责人明确同意后，才将候选代码合并到 `main`，同步正式后端 `APP_VERSION`，部署正式目录并创建版本 tag。

未经明确验收，不得执行第 5 步。

## 不递增版本号的小补丁

- 可在完成针对性验证和备份后直接发布。
- 不触发后端版本强制更新，不创建新版本 tag。
- 若补丁涉及入口 HTML、构建工具、缓存、认证、数据库结构或 Nginx，仍必须先走 `test`。

## 静态资源部署红线

- 发布新构建时只能合并新增资源，禁止清空或整体替换线上 `assets/`。
- 至少保留当前正式版和上一正式版所引用的哈希资源。
- 背景、字体等非关键资源不得使用 `fetchpriority="high"` 抢占主程序带宽。
- 图片优化不得改变像素或强制所有用户冷下载；必须验证企业微信 Wi-Fi、4G 和弱网。
- 测试环境与正式环境使用独立入口和资源目录，测试版不得修改正式版后端权威版本号。

## 回滚

- 每次部署前备份目标目录和相关 Nginx 配置。
- 正式版异常时恢复上一正式 tag 对应目录，不在故障现场继续叠加修复。
- 回滚后核对入口文件、后端版本号、Nginx 配置和 GitHub `main` 内容。
